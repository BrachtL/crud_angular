// src/auth/dto/login.dto.ts
/*
import { IsEmail, IsNotEmpty, Length } from 'class-validator';

export class LoginDto {
  @IsEmail()
  @IsNotEmpty()
  email: string;

  @IsNotEmpty()
  @Length(6, 20) // Require password to be between 6 and 20 characters
  password: string;
}
*/